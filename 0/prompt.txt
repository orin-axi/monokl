Run the "deep-research" workflow.

Deep research harness — fan-out web searches, fetch sources, adversarially verify claims, synthesize a cited report.

When the user wants a deep, multi-source, fact-checked research report on any topic. BEFORE invoking, check if the question is specific enough to research directly — if underspecified (e.g., "what car to buy" without budget/use-case/region), ask 2-3 clarifying questions to narrow scope. Then pass the refined question as args, weaving the answers in.

Phases:
- Scope: Decompose question (from args) into 5 search angles
- Search: 5 parallel WebSearch agents, one per angle
- Fetch: URL-dedup, fetch top 15 sources, extract falsifiable claims
- Verify: 3-vote adversarial verification per claim (need 2/3 refutes to kill)
- Synthesize: Merge semantic dupes, rank by confidence, cite sources

Invoke: Workflow({ name: "deep-research", args: "Deeply research colbymchenry/codegraph (docs site: https://colbymchenry.github.io/codegraph/, repo: https://github.com/colbymchenry/codegraph) — fully understand its architecture, design decisions, and claims, with adversarial verification of any benchmark/performance claims rather than taking marketing copy at face value.\n\nContext: I'm designing \"monokl\", a Rust crate for AST-aware semantic code search over TypeScript/JavaScript (via OXC) and Rust (via ra-ap-syntax), aimed at AI coding agents as the primary consumer. Its pipeline: ripgrep-style text prefilter → custom BM25 ranking (K1=1.5, B=0.5) → node-type/coverage boosts → dedup → token-budget truncation (tiktoken o200k_base). Two-level architecture: per-file LanguageAnalyzer producing owned FileAnalysis (symbols/deps/exports/code-blocks/JSX), folded by WorkspaceEnricher into a WorkspaceIndex (import graph + symbol index). Two-tier cache invalidation (mtime/size stat fast-path, then blake3 content-hash), no persistent on-disk search index by design. ~20 CLI commands (search/symbols/extract/dependents/inspect/patterns/refs/definition/diff/tokens/explain/coverage/data-flow/similar/deps/deps-from/classify/migration-surface) plus PR-aware git scopes (gix-based, FullRepo/ChangedFiles/ChangedLines/ImpactedNeighbors) and a presentation layer (json/minimal/text/mermaid/schema). Multi-language support via an AnalyzerRegistry with per-analyzer capability/precision levels for graceful degradation. JSON-first output with JSON Schema per result type and field projection (--only/--fields).\n\nResearch questions to answer thoroughly:\n1. What exactly is codegraph's architecture (parsing approach, storage engine, indexing/incremental-update strategy, ranking/retrieval algorithm, MCP tool surface design)? Read actual source, not just docs/README claims.\n2. What are its published benchmark claims (tool-call reduction, token savings, cost, latency) — are the methodology and numbers credible? What repos/tasks were tested, how many runs, what's the variance?\n3. How does it handle staleness/incremental re-indexing, cross-file reference resolution, and multi-language support in practice?\n4. What is its MCP tool design philosophy and why (there's apparently a documented rationale for exposing few vs. many tools to agents) — is that rationale well-supported by evidence or just asserted?\n5. What do users/reviewers/discussions (GitHub issues, HN, Reddit, blog posts, X/Twitter if findable) say about it in practice — real-world strengths, complaints, limitations?\n6. How does its overall design compare to monokl's design described above — where is codegraph's approach genuinely more mature/validated, and where does monokl's design (native OXC/ra-ap-syntax parsing precision, BM25+token-budget pipeline, capability-driven multi-language degradation, general git-scope composability, JSON Schema contracts) look stronger on paper?\n\nProduce a cited, adversarially-checked report with concrete, actionable recommendations for monokl's design — cite sources for every non-obvious claim, and flag anything that's marketing assertion vs. independently verifiable." })